//- if(errors)
//-   extends layout

//-   block content
//-     h1= message
//-     h2= error.status
//-     pre #{error.stack}

if(errors)
  h2.errors Oops an error occurred!
  ul.errors
    each error in errors
      li
        =error.message